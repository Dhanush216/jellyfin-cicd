grafana:
  admin:
    existingSecret: grafana-admin-secret
    userKey: admin-user
    passwordKey: admin-password
  service:
    type: NodePort
    nodePort: 31380

prometheus:
  service:
    type: NodePort
    nodePort: 32001